// Task 2: Add Event Listeners for Product Selection

sizeDropdown.addEventListener {
const selectedSize ();
const updatedPrice ();
}

// Task 3: Handle Stock Availability
const pricemap = {
    small: 20
    medium: 30
    large: 40
};

const sizeDropdown = .getElementById('size-options');
}const sizeDropdown = .getElementById('product price');
if ( 'out-of-stock') {
    purchaseButton.disabled = true;
    purchaseButton.Content = 'Out of Stock';